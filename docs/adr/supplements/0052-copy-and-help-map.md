# ADR-0052 付録B — UIコピー正語＆ヘルプ 詳細対応表

> [ADR-0052](../0052-ui-copy-terminology-unification-and-help.md) の付随資料（ADR-0050 原則5）。
> 決定・方針・横断辞書（付録A）は ADR 本体。ここは画面セグメント別の**確定コピー**と**ヘルプ本文**。
> 用語の現行の正は `docs/reference/ubiquitous-language.md` の「UI 用語統一表」に反映して運用する。
>
> 各表の見方: 「コピー変更表」＝変更が必要な文言のみ（`変更なし` は各節末に列挙）。
> 「正語(通常)」＝企画/開発者向け、「正語(end_user)」＝利用者向け（`同左`＝同一）。`{…}` は動的差し込み。

---

## S1. 入口フロー
対象: `components/EntryFlow.tsx`（01ホーム/02準備）・`app/login/page.tsx`・`components/ConversationStart.tsx`（03会話開始）

### コピー変更表

| 現状の文言 | 正語(通常) | 正語(end_user) | 位置・表示条件 |
|---|---|---|---|
| `会議の前に、五分の問答を` | `会議の前に、五分の会話を` | 同左 | 01ホーム ヒーロー見出し(h1)。問答→会話 |
| `＋ 壁打ちを始める` | `＋ 要件インタビューを始める` | `＋ 聞き取りを始める` | 01ホーム 主要CTA。壁打ち排除＝開始行為の正語に統一 |
| `対象のアプリを選ぶと壁打ちを始められます。` | `対象のアプリを選ぶと要件インタビューを始められます。` | `対象のアプリを選ぶと聞き取りを始められます。` | 01ホーム アプリ未選択時の補助文 |
| `このアプリは URL キーワードが未設定のため、壁打ちを始められません。` | `…要件インタビューを始められません。` | `…聞き取りを始められません。` | 01ホーム slug未設定時の補助文 |
| `対象のプロダクト・アプリ` | `対象のアプリ` | 同左 | 01ホーム アプリ選択フィールドのラベル（02準備と表記統一＋プロダクト除去） |
| `対象のアプリを選ぶと、その用語や前提コードを問いの背景に取り込みます。` | `対象のアプリを選ぶと、その用語や前提リポジトリのコードを会話の質問の背景に取り込みます。` | 同左 | 01ホーム アプリ選択 hint。問い→質問、前提コード→前提リポジトリのコード |
| `要件サンバを始める`（可視＋aria） | `要件インタビューを始める` | `聞き取りを始める` | 02準備 主要CTA。既定役割=利用者のため既定表示は end_user 側（役割で切替） |
| `連携リポジトリ（任意）` | `前提リポジトリ（任意）` | 同左 | 02準備 リポジトリ選択ラベル（select版・手入力版とも） |
| `連携しない`（select 先頭） | `紐づけない` | 同左 | 02準備 リポジトリ未選択の選択肢。フィールド名に合わせ統一 |
| `確定した要件を GitHub Issue として起票する先。Issue/README は問いの文脈にも使われます。` | `…Issue/README は会話での質問の前提にも使います。` | 同左 | 02準備 前提リポジトリ hint |
| `owner/name 形式で入力（候補一覧を取得できなかったため手入力）。空欄で連携しない。` | `…空欄で紐づけない。` | 同左 | 02準備 手入力版 hint |
| `開始時にこのブランチの内容を索引し、問いの前提として使います（既定はデフォルトブランチ）。` | `…会話の質問の前提として使います（既定はデフォルトブランチ）。` | （利用者画面には出さない） | 02準備 ブランチ hint（開発者向け）。索引は語を維持しヘルプで補足 |
| `ゴールを入力・・・` | `ゴールを入力…` | 同左 | 02準備 ゴール placeholder。中黒3つ→三点リーダに統一 |
| `背景・現状・制約・期待する成果などを自由に入力・・・` | `…自由に入力…` | 同左 | 02準備 ゴールの詳細 placeholder。中黒3つ→三点リーダ |
| `支度、相整いまして` | `準備ができました` | 同左 | 03-0 開始前ヘッダ。演出語→機能名 |
| `問答を始める支度が整いました。` | `会話を始める準備ができました。` | 同左 | 03-0 開始前サブ文 |
| `問答を始める`（可視） | `会話を始める` | 同左 | 03-0 開始前 主要CTA（aria は既に「音声で会話を始める」）。※02のセッション開始CTAとは別段階（音声会話の開始） |
| `音声で問答するためマイクを使用します。次の画面で許可を求めます。` | `音声で会話するため…` | 同左 | 03-0 マイク理由提示 |
| `声を聞かせてくださいませ` | `マイクの使用を許可してください` | 同左 | 03-2 マイク許可モーダル見出し |
| `問答には端末のマイクを用います。使用を許可してください。` | `会話には端末のマイクを使います。使用を許可してください。` | 同左 | 03-2 本文。問答→会話、用います→使います |
| `繋いでおります` | `接続しています` | 同左 | 03-1 接続中ヘッダ |
| `繋いでおります…` / `繋ぎ直しております…` | `接続しています…` / `再接続しています…` | 同左 | 03-1 接続中の状態文 |
| `繋ぎ直しております… しばらくお待ちください` | `再接続しています… しばらくお待ちください` | 同左 | 会話中の再接続オーバーレイ帯（RoomGate） |
| `繋ぐことが叶いませなんだ` / `声を捉えられませなんだ` | `接続できませんでした` / `マイクを認識できませんでした` | 同左 | 03-3 失敗見出し（connect／mic） |
| `ルームに参加` | 維持 | `通話に参加` | 03-1 接続ステップ。ルーム=LiveKit用語、利用者向けのみ平易化（P2・任意） |
| `音声を確立中` | 維持 | `音声を準備中` | 03-1 接続ステップ。利用者向けのみ平易化（P2・任意） |
| `SANBA の起動を待機` | 維持 | `SANBA の準備を待っています` | 03-1 接続ステップ。利用者向けのみ平易化（P2・任意） |
| `問答の間へ、ようこそ` | `SANBA へようこそ` | 同左 | 11ログイン 左ブランドペイン見出し |
| `✦ またのお越しを ✦` | `またのお越しを` | 同左 | 14ログアウト完了 eyebrow。装飾記号 ✦ を削除（文言は維持） |
| `ログアウトしました。問答の記録は安全に保たれています。` | `ログアウトしました。会話の記録は安全に保たれています。` | 同左 | 14ログアウト完了 本文 |

**変更なし（確認済み）**: 役割ラベル `利用者／企画者／開発者`（`AUDIENCE_LABELS`＝辞書一致）／`参考資料（任意）`／`ファイルを追加`／
`会話を始められませんでした`（03-3ヘッダ・既に「会話」）／`音声で会話を始める`（aria）／`サインイン`/`サインアップ`/`本人確認`/`キャンセル`/
`もう一度試す`/`設定を開いて許可する`/`準備しています…`/`確認しています…`/`選択してください`/`ログインへ`/`おつかれさまでした`／
`一問ずつ問いかけ、抜けと矛盾をその場で取り上げます。`（現代語のため維持）。

### ヘルプ追加ポイント

| 位置（ラベルの隣・画面） | 見出し | 本文 | 対象 |
|---|---|---|---|
| 02準備「前提リポジトリ（任意）」 | 前提リポジトリとは | 紐づけたコードを読み込み、会話での質問の前提に使います。確定した要件を GitHub の Issue として書き出す先にもなります。 | 通常（利用者にも表示され得る） |
| 02準備「ブランチ」（App連携時） | コードの読み込み（索引）とは | リポジトリの中身をあらかじめ読み込み、検索できる状態にすることです。会話中の質問の前提として使います。 | 通常（開発者向け） |
| 02準備 同意チェック | 録音とデータの扱い | 会話は録音し、内容の整理に AI を使います。録音と要件は最大30日で自動的に削除し、保存前に個人情報を伏せ字にします。 | 通常・end_user（「最大30日」は `RETENTION_DAYS` の実値に合わせる） |
| 01ホーム「対象のアプリ」 | 対象のアプリとは | 会話の対象にするアプリです。選ぶと、そのアプリの用語や前提リポジトリのコードを会話の前提に取り込みます。 | 通常・end_user |

---

## S2. 会話・選択肢・検知
対象: `ConversationShell`/`ChatHistory`/`BottomBar`/`VoiceStatusIndicator`/`sanba/VoiceInputBar`/`sanba/Waveform`/`sanba/ChatBubble`/`ConversationSessionView`/`SessionView`/`EndConfirmDialog`/`ChoiceStrip`/`ChoiceDetailSheet`/`ChoiceCompareSheet`/`ChoicePin`/`DetectionPin`

### コピー変更表

| 現状の文言 | 正語(通常) | 正語(end_user) | 位置・表示条件 |
|---|---|---|---|
| `問答` | `会話` | 同左 | ConversationShell ヘッダ画面タイトル h1（会話中・常時） |
| `要件絵巻` | `要件一覧` | 同左 | ConversationShell タブラベル（scroll）。※S4「要件一覧」と整合 |
| `未確定` | `未解消` | 同左 | ConversationShell ミニ状況の中央ラベル（`⚠ 未確定 {n}`）。EndConfirmDialog／確定エラー文の「未解消」と表記統一（多数派・07判定に揃える） |
| `資料` | `参考資料` | 同左 | ConversationShell ミニ状況の右ラベル（`📎 資料 {n}`）。末尾「（解析中）」は維持。※S3 と整合 |
| `問答を終えますか？` | `会話を終えますか？` | 同左 | EndConfirmDialog 見出し |
| `問答を続ける` | `会話を続ける` | 同左 | EndConfirmDialog 続行ボタン |
| `● 認識中...（あなたが発話中）` | `● 聞き取り中` | 同左 | VoiceInputBar 既定ステータス（listening）。「認識」を平易化し VoiceStatusIndicator「聞き取り中」と統一。※本部品は現状 `/design` 専用で会話本編は VoiceStatusIndicator を使用（本編に影響なし） |
| `どう並ぶ？` | `位置づけ`（要確認） | `ほかとの違い`（要確認） | ChoiceDetailSheet 観点見出し（`how`）。修辞的な問い形→名詞ラベル。※`how` の意味を実装と要確認のうえ確定 |

**変更なし（確認済み）**: `会話履歴`/`参考資料`（タブ）／`聞き取り中`・`発話中／読み上げ中`・`消音中`・`待機中`（VoiceStatusIndicator）／
`集音中`・`ミュート中`（BottomBar マイクトグル・Waveform aria＝機器のON/OFF状態でフェーズ「聞き取り中」とは別概念のため意図的に保持）／
`消音`・`消音中`（BottomBar 出力トグル）／`テキストで入力…`/`送信`／`文字起こし中…`/`話しかけてください`／
`選択肢の詳細`・`選択肢を見比べる`・`広げる`・`閉じる`・`詳細`・`比較`・`選ぶ`・`前の選択肢`・`次の選択肢`・`「〜」を選ぶ`／
検知バッジ `矛盾`・`抜け`・`不明瞭`（食い違い・確認・あいまい＝mapping.ts 現行維持）／SessionView 各種エラー文／
`確定できませんでした。未解消の項目が…`（ConversationSessionView）／観測オーバーレイ `受信・重複・破棄・欠番`（aria-hidden・開発用）／
ChatBubble 既定グリフ `産`/`企`（aria は既に「SANBA」「あなた」。中立化は P2・任意）。

### ヘルプ追加ポイント

| 位置（ラベルの隣・画面） | 見出し | 本文 | 対象 |
|---|---|---|---|
| 「効き目」（ChoiceDetailSheet/ChoiceCompareSheet） | 効き目とは | この選択肢を選ぶと得られる良い点です。ほかの案と比べて何が良くなるかを示し、選ぶときの判断材料にします。 | 通常 |
| 同上 | 効き目とは | この選択肢を選ぶと、どんな良いことがあるかです。ほかの案と比べて良くなる点を示します。 | end_user |
| 「留意」（同上） | 留意とは | この選択肢を選ぶ前に気をつけたい点です。見落としやすい注意点や、引き換えに諦めることを示します。 | 通常 |
| 同上 | 留意とは | この選択肢を選ぶときに、気をつけておきたい点です。あとで困らないための注意点を示します。 | end_user |
| 「関連・出所」（ChoiceDetailSheet） | 関連・出所とは | この選択肢の根拠にした発言や参考資料です。会話のどの発言や、どの資料にもとづくかを示します。 | 通常 |
| 同上 | だれの発言かとは | この案が、だれの発言やどの参考資料から出てきたかを示します。 | end_user |
| 「位置づけ」（旧「どう並ぶ？」） | 位置づけとは | ほかの選択肢と比べて、この案がどこに位置するかを示します。並び順や方向性のちがいを見比べる手がかりです。（※`how` の意味を実装と要確認） | 通常・end_user |
| 検知バッジ「矛盾」（ChoiceStrip/DetectionPin） | 矛盾とは | 会話の中で、前に話した内容と後の内容が食い違っている点です。どちらが正しいかを確認すると、要件を確定できます。 | 通常 |
| 同上（「食い違い」） | 食い違いとは | 前にお話しした内容と、後のお話がずれている点です。どちらが正しいか教えていただくと、先に進めます。 | end_user |
| 検知バッジ「抜け」 | 抜けとは | まだ決まっていない、または話に出ていない項目です。ここを埋めると要件がそろい、確定できます。 | 通常 |
| 同上（「確認」） | 確認したい点とは | まだ決まっていない、またはお聞きできていない点です。ここを教えていただくと、内容がそろいます。 | end_user |
| 検知バッジ「不明瞭」 | 不明瞭とは | 複数の意味に受け取れて、はっきりしない論点です。言い換えや具体例で意味を一つに絞ると確定できます。 | 通常 |
| 同上（「あいまい」） | あいまいとは | いくつかの意味に取れて、はっきりしない点です。もう少し具体的に教えていただくと、はっきりします。 | end_user |
| ミニ状況「未解消」（ConversationShell） | 未解消とは | 会話でまだ解消していない検知（食い違い・抜け・あいまい）の件数です。残っていると要件を確定できません。 | 通常・end_user |

---

## S3. 参考資料
対象: `MaterialsList`/`MaterialDetailSheet`/`MaterialSourceSheet`/`MaterialCancelDialog`

### コピー変更表

| 現状の文言 | 正語(通常) | 正語(end_user) | 位置・表示条件 |
|---|---|---|---|
| `素材を追加（カメラ・アップロード・画面共有）` | `参考資料を追加（カメラ・アップロード・画面共有）` | 同左 | MaterialsList 追加ボタン |
| `まだありません。資料を追加すると、ここに解析状況が出ます。` | `まだありません。参考資料を追加すると、ここに解析状況が出ます。` | 同左 | MaterialsList 空状態 |
| （aria）`資料 {name} の詳細を開く` | `参考資料 {name} の詳細を開く` | 同左 | MaterialsList done 行ボタン aria |
| （aria）`資料 {name}` | `参考資料 {name}` | 同左 | MaterialsList 非done 行 aria |
| `資料の詳細` | `参考資料の詳細` | 同左 | MaterialDetailSheet シート見出し |
| `資料の追加方法を選ぶ` | `参考資料の追加方法を選ぶ` | 同左 | MaterialSourceSheet 見出し |
| （aria）`資料の追加方法` | `参考資料の追加方法` | 同左 | MaterialSourceSheet dialog aria |
| `写真（PNG/JPG）・録画（MP4/MOV）・資料（PDF/Office/Markdown/HTML/CSV 等）` | `写真（PNG/JPG）・録画（MP4/MOV）・文書（PDF/Office/Markdown/HTML/CSV 等）` | 同左 | MaterialSourceSheet「ファイルをアップロード」sub。ここの「資料」は傘概念「参考資料」と衝突するため「文書」に置換（用語衝突回避） |
| `言葉×画の矛盾` | 変更なし | `言葉と画像の食い違い` | MaterialDetailSheet セクション見出し＋バッジ（2箇所）。end_user のみ辞書「矛盾→食い違い」に合わせ平易化 |
| `言葉×画の矛盾は見つかっていません。` | 変更なし | `言葉と画像の食い違いは見つかっていません。` | MaterialDetailSheet 空状態。end_user のみ平易化 |

**変更なし（確認済み）**: 状態語 `解析中`/`解析済`/`解析の進捗`/`アップロード中`/`{pct}%`/`アップロード/解析に失敗`/`再試行`／
中断フロー `中断`/`中断しますか？`/`「{name}」の解析を中断します。途中までの結果は破棄されます。`/`続ける`/`中断する`／
`抽出した要件`/`抽出された要件はありません。`/`会話で確認`（既に「会話」）/`解析結果はこの場では取得できていません。`/`解析が終わると、ここに表示されます。`／
`言葉以外の情報（画像・画面・カメラ）も、会話を止めずに渡せます。`/`カメラで撮影`/`ファイルをアップロード`/`画面を共有`/`画面共有を停止`/
`Google ドライブから選ぶ`/準備中案内/`キャンセル`/`ON`/`閉じる`/`閉じる（背景）`。

### ヘルプ追加ポイント

| 位置（ラベルの隣・画面） | 見出し | 本文 | 対象 |
|---|---|---|---|
| 「解析中」ステータス（MaterialsList各行/DetailSheet メタ） | 解析とは | 追加した参考資料の中身をAIが読み取り、要件の抽出や画像との照合に使える形に整理する処理です。会話を止めず、バックグラウンドで進みます。 | 通常 |
| 同上 | 解析とは | 追加した参考資料の中身をAIが読み取り、話し合いに使える形に整える作業です。会話を止めず、うしろで少しずつ進みます。 | end_user |
| 「言葉×画の矛盾」見出し（DetailSheet） | 言葉×画の矛盾とは | 会話で話した内容と、画像や画面などの参考資料の中身が食い違っている箇所です。SANBAが自動で見つけ、会話で確認するきっかけにします。 | 通常 |
| 「言葉と画像の食い違い」見出し（DetailSheet） | 言葉と画像の食い違いとは | 話した内容と、写真や画面などの中身が合っていないところです。SANBAが自動で見つけて、あとで会話で確かめられるようにします。 | end_user |
| 「参考資料を追加」/空状態（MaterialsList） | 参考資料とは | 会話の前提としてSANBAに渡す、画像・録画・画面・文書などの補足情報です。中身をAIが読み取り、要件の抽出や確認に使います。 | 通常 |
| 同上 | 参考資料とは | 会話の助けにするために渡す、写真・録画・画面・書類などのことです。中身をAIが読み取って、話し合いに役立てます。 | end_user |
| 「抽出した要件」見出し（DetailSheet） | 抽出した要件とは | 参考資料の中から、要件になりそうな内容をAIが自動で拾い出したものです。会話で確認しながら、確定した要件に整えていきます。 | 通常 |
| 同上 | 抽出した要件とは | 参考資料の中から、要件になりそうな内容をAIが自動で拾い出したものです。会話で確かめながら、まとめていきます。 | end_user |

---

## S4. 要件一覧・深掘り・判定
対象: `RequirementsTab`/`RequirementsScrollList`/`DeepDiveList`/`sanba/RequirementCard`/`sanba/InsightCard`/`JudgmentGate`

### コピー変更表

| 現状の文言 | 正語(通常) | 正語(end_user) | 位置・表示条件 |
|---|---|---|---|
| `要件絵巻（MoSCoW・確信度/出所つき・閲覧のみ）` | `要件一覧（MoSCoW・確信度/出所つき・閲覧のみ）` | （end_user は既存の平易見出しを表示） | RequirementsTab h2（通常）。絵巻→一覧 |
| `まだ要件はありません。問答が進むと、ここに育っていきます。` | `まだ要件はありません。会話が進むと、ここに整理されていきます。` | 同左（会話中 end_user は親から別文言） | RequirementsScrollList 既定 emptyText。問答→会話＋比喩排除 |
| `確信 高　・　出所 {話者}` | `確信度 高　・　出所 {話者}` | `確からしさ 高　・　だれの発言か {話者}` | RequirementsScrollList メタ行。モード分岐を追加。値 高/中/低 と全角区切りは維持 |
| `未解消はありません（すべて解けました）。` | `未解消はありません（すべて確認できました）。` | `確認したいことはありません。` | DeepDiveList 空状態。モード分岐を追加 |
| `改める` | `修正する` | 同左 | RequirementCard 操作ボタン（onRevise） |
| `認める` | `承認する` | 同左 | RequirementCard 操作ボタン（onApprove）。状態チップ「承認済み」と整合 |
| `退ける` | `却下する` | 同左 | RequirementCard 操作ボタン（onReject）。状態チップ「却下」と整合 |
| `ひらめき` | `気づき` | 同左 | InsightCard 既定 title。愛称→機能名（insight）。※軽微・要否は要判断 |
| `見極めの刻にございます` | `内容の確認` | 同左 | JudgmentGate 見出し（両モード） |
| `すべて解けました。要件を確定できます。` | `すべて確認できました。要件を確定できます。` | （end_user は別文言・変更なし） | JudgmentGate 解消済み説明（通常） |
| `ひとつでも残れば、要件は確定できませぬ。` | `ひとつでも残れば、要件は確定できません。` | （end_user は別文言・変更なし） | JudgmentGate 未解消補足（通常）。古語平叙化 |
| `問答に戻って解く` | `会話に戻って確認する` | （end_user は「会話に戻って答える」・変更なし） | JudgmentGate 未解消 主ボタン（通常） |
| `未解消のまま終う` | `未解消のまま終える` | （end_user は「このまま終える」・変更なし） | JudgmentGate 未解消 副ボタン（通常）。終う→終える |

**変更なし（確認済み）**: RequirementsTab end_user 見出し `うかがった内容の整理（閲覧のみ）`／end_user 空状態 `まだありません。お話が進むと、ここに整理されていきます。`／
end_user 深掘り見出し `確認したいこと（残り {n}）`／通常 深掘り見出し `深掘り対象（未解消 {n}）`／DeepDiveList `会話で確認`／
JudgmentGate `未解消 {n} 件 ・ 確定不可`/`要件を確定する`/`この内容で伝える`/`確認したいことが {n} 件残っています`/
`確認したいことは残っていません。この内容でお伝えできます。`/`会話に戻ってお答えいただくと、より正確に伝わります。`/`会話に戻って答える`/`このまま終える`／
RequirementCard 状態チップ `下書き`/`承認済み`/`却下`、確信度値 `高`/`中`/`低`。

### ヘルプ追加ポイント

| 位置（ラベルの隣・画面） | 見出し | 本文 | 対象 |
|---|---|---|---|
| メタ行「確信度」（要件一覧） | 確信度とは | その要件をどれくらい確からしいと判断したかの度合いです。会話の内容から自動で推定します。 | 通常 |
| メタ行「確からしさ」（要件一覧） | 確からしさとは | その内容がどれくらい確かかを表す度合いです。会話の中身から自動で見積もります。 | end_user |
| メタ行「出所」（要件一覧） | 出所とは | その要件が、会話の中でだれの発言をもとにまとめられたかを示します。利用者・企画者・開発者のいずれかが入ります。 | 通常 |
| メタ行「だれの発言か」（要件一覧） | だれの発言かとは | その内容を、会話の中でどなたの話をもとにまとめたかを示します。 | end_user |
| 「MoSCoW」/優先度区分見出し | MoSCoW（優先度）とは | 要件を重要度で四段階に分ける考え方です。Must（必須）・Should（望ましい）・Could（できれば）・Won't（今回は対象外）の頭文字をとった呼び方です。 | 通常 |
| 優先度区分見出し（要件一覧） | 優先度とは | その要件を今回どれくらい優先するかの分け方です。「ぜひ必要」「あると助かる」「できれば」「今回は見送り」の4つに分けています。 | end_user |
| RequirementsTab「深掘り対象」見出し | 深掘り対象とは | 会話の中で見つかった、矛盾・抜け・不明瞭など、確認しておきたい点です。会話に戻って解消できます。 | 通常 |
| RequirementsTab「確認したいこと」見出し | 確認したいこととは | お話の中で見つかった、食い違いや足りない点など、もう少し確かめたいところです。会話に戻ってお答えいただけます。 | end_user |
| JudgmentGate 見出し「内容の確認」 | 内容の確認（判定）とは | 終了する前に、未解消の点が残っていないかをまとめて見直す画面です。すべて解消できていれば、要件を確定できます。 | 通常 |
| JudgmentGate 見出し「内容の確認」 | 内容の確認とは | 終わる前に、確かめたいことが残っていないかを見直す画面です。残りがなければ、この内容でお伝えできます。 | end_user |

---

## S5. 結果・過去の要件一覧
対象: `ResultView`/`app/results/[id]/page.tsx`/`app/results/page.tsx`/`sanba/SessionHistoryList`/`sanba/SessionRow`

### コピー変更表

| 現状の文言 | 正語(通常) | 正語(end_user) | 位置・表示条件 |
|---|---|---|---|
| `オーレ！ 要件、産まれました` | `要件がまとまりました` | （変更なし：既存「お話の内容を整理できました」） | ResultView 完了見出し／確定時。通常のみ演出語 |
| `この絵巻を画面で確認する` | `全文を確認する` | 同左 | ResultView 主ボタン本文（onView） |
| `この絵巻を画面で確認する（確定要件の全文）` | `確定要件の全文を確認する` | `確認できた内容の全文を確認する` | ResultView 主ボタン aria。end_user は「確定要件」を平易語へ |
| `新しい問答を始める` | `新しい会話を始める` | 同左 | ResultView 最下部リンク（onRestart） |
| `うかがった内容 {N} 件` | （変更なし：既存「確定要件 {N} 件／暫定要件 {N} 件」） | `確認できた内容 {N} 件`（確定時）／`途中までの内容 {N} 件`（暫定時） | ResultView 件数サマリ／end_user のみ出し分け追加 |
| `確定要件のプレビュー` | 変更なし | `確認できた内容のプレビュー` | ResultView プレビュー aria（sr-only）。end_user のみ（P2） |
| `確定済み`（sr-only） | 変更なし | `確認できた` | ResultView 各要件末尾チェック sr-only。end_user のみ（P2） |
| `要件絵巻` | `要件一覧` | 同左 | /results/[id] 画面タイトル（AppShell title） |
| `このセッションの要件はまだ生まれておりませぬ。` | `このセッションの要件はまだありません。` | 同左 | /results/[id] 空状態。古語平叙化＋擬人化排除 |
| `再び試みる` | `もう一度試す` | 同左 | /results/[id] 再試行ボタン。他画面の表記に統一 |
| `過去の要件はまだございません。壁打ちを始めると、ここに残ります。` | `過去の要件はまだありません。要件インタビューを始めると、ここに残ります。` | `…聞き取りを始めると、ここに残ります。` | SessionHistoryList 空状態 |
| `過去の要件を見る` | `要件一覧` | 同左 | SessionHistoryList 見出し h2。※ページ見出し「過去の要件一覧」と重複するため要否はレイアウトで再検討可 |
| `検める ›` | `確認する ›` | 同左 | SessionRow 操作ピル既定。`›` は機能記号のため維持 |

**変更なし（確認済み）**: /results 画面タイトル `過去の要件一覧`／ResultView 通常件数 `確定要件 {N} 件`/`暫定要件 {N} 件`/`・ 未確定を残したまま終了`/`暫定で書き留めました`／
end_user 完了見出し `お話の内容を整理できました`/`ここまでのお話を書き留めました`／集計 `矛盾解消 … ・ 抜け検知 … ・ Issue 起票 …`／
`書き出す（任意）`/`ほか {N} 件 ・ タップで全文`/`{kind} を開く`／Issue系 `起票中…`/`起票した Issue を開く ↗`/`Issue を起票しました` と失敗理由文／
/results/[id] `結果ドキュメントを出力`＋説明文/`{利用者/企画者/開発者}向け`/`確定済み・未確定` メタ/`整形中…`/`ドキュメントの生成に失敗しました。…`/
`デフォルトのフォーマットで出力しています（…）。`/`コピーしました`・`Markdown をコピー`/`読み込み中…`/読み込み・認証エラー文/`ログインへ`。

### ヘルプ追加ポイント

| 位置（ラベルの隣・画面） | 見出し | 本文 | 対象 |
|---|---|---|---|
| ResultView 件数「確定要件／暫定要件」＋ /results/[id]「確定済み／未確定」 | 確定要件と暫定要件とは | 確定要件は会話の中で合意できた項目です。暫定要件はまだ確認しきれていない項目で、未確定を残したまま終了したときに残ります。 | 通常 |
| ResultView 件数（end_user）「確認できた内容／途中までの内容」 | 確認できた内容と途中までの内容とは | 確認できた内容は、お話の中ではっきり決まった項目です。途中までの内容は、まだ確かめきれていない項目です。 | end_user |
| /results/[id]「結果ドキュメントを出力」見出し | 出力フォーマットとは | 要件結果を文書にするときの体裁です。読み手（利用者・企画者・開発者）ごとに切り替えられます。アプリ管理画面で登録でき、登録がないときはデフォルトを使います。 | 通常 |
| ResultView 出力ボタン「Issue」 | Issue の起票とは | 確定した要件を GitHub の Issue として書き出すことです。開発の作業項目として引き継げます。GitHub 連携が有効で、許可されたリポジトリにだけ書き出せます。 | 通常 |
| /results/[id] 要件内「確信」（共有 `RequirementsScrollList`・S4 と重複するため実装は共通化） | 確信度とは | その要件をどれくらい確からしいと判断したかの度合いです。会話の内容から自動で推定します。 | 通常 |
| 同上（end_user「確からしさ」） | 確からしさとは | その内容がどれくらい確かかを表す目安です。お話の内容から自動で見積もります。 | end_user |

---

## S6. アプリ管理
対象: `app/products/page.tsx`/`app/products/[id]/page.tsx`/`ProductRepoCard`/`ProductMembersCard`/`ProductInvitesCard`/`ProductCheckItemsCard`/`ProductOutputFormatsCard`
（ヘルプは全て**通常**＝オーナー向け画面。end_user には表示されない）

### コピー変更表

| 現状の文言 | 正語(通常) | 位置・表示条件 |
|---|---|---|
| `深掘りの対象になるアプリです。登録すると、リポジトリの紐づけと深掘りリンクの発行ができます。` | `要件インタビューの対象になるアプリです。登録すると、前提リポジトリの紐づけと要件インタビューのリンクの発行ができます。` | /products「アプリを登録」説明文 |
| `壁打ちの URL（/キーワード/prepare）になります。…` | `要件インタビューの URL（/キーワード/prepare）になります。…` | /products URLキーワード hint |
| `あなたはこのアプリのメンバーです。ホームの「壁打ちを始める」から対象にこのアプリを選ぶと、要件サンバを始められます。` | `…ホームの「要件インタビューを始める」から対象にこのアプリを選ぶと、要件インタビューを始められます。` | /products/[id]（メンバー閲覧）説明文 |
| `要件サンバを始める` | `要件インタビューを始める` | /products/[id]（メンバー閲覧）ボタン |
| `壁打ちの URL は /{slug}/prepare です。変更すると URL も変わります。` | `要件インタビューの URL は /{slug}/prepare です。変更すると URL も変わります。` | /products/[id] URLキーワード hint（設定済み） |
| `未設定です。設定するまでこのアプリでは壁打ちを始められません（/キーワード/prepare が壁打ちの URL になります）。` | `未設定です。設定するまでこのアプリでは要件インタビューを始められません（/キーワード/prepare が要件インタビューの URL になります）。` | /products/[id] URLキーワード hint（未設定） |
| `利用者に見えている言葉…。利用者向けの深掘りではこの語彙で質問し、技術用語を使いません。` | `…利用者向けの要件インタビューではこの語彙で質問し、技術用語を使いません。` | /products/[id]「利用者向け語彙」説明文 |
| `削除すると発行済みの深掘りリンクもすべて使えなくなります。` | `削除すると発行済みの要件インタビューのリンクもすべて使えなくなります。` | /products/[id]「アプリの削除」説明文 |
| `紐づけたリポジトリの内容を索引し、このアプリの深掘りセッションの前提として使います。` | `紐づけたリポジトリの内容を索引し、このアプリの要件インタビューの前提として使います。` | ProductRepoCard 説明文（索引は語を維持しヘルプ補足） |
| `このアプリで要件サンバをできる人です。` | `このアプリで要件インタビューができる人です。` | ProductMembersCard 説明文先頭（後続は変更なし） |
| `深掘りリンク`（CardTitle） | `要件インタビューのリンク` | ProductInvitesCard 見出し（メンバー招待「招待リンク」と衝突しない語） |
| `URL を開くだけでこのアプリの深掘りセッションを始められるリンクです。…` | `URL を開くだけでこのアプリの要件インタビューを始められるリンクです。…` | ProductInvitesCard 説明文 |
| `開発者向け（技術の深掘り）` | `開発者向け（技術面をくわしく確認）` | ProductInvitesCard「対象」developer 選択肢（end_user 選択肢は変更なし） |
| `深掘りリンクの取得に失敗しました` | `要件インタビューのリンクの取得に失敗しました` | ProductInvitesCard 読み込み失敗エラー |
| `要件サンバ中の確認項目` | `要件インタビュー中の確認項目` | ProductCheckItemsCard 見出し |
| `…産婆さんが会話の流れの中で一つずつ確認し、要件結果の文書にも一覧が載ります。` | `…会話の流れの中で一つずつ確認し、要件結果の文書にも一覧が載ります。` | ProductCheckItemsCard 説明文。擬人化主語「産婆さんが」削除（「セッション」は維持） |
| `出所・引用はセッションの要件絵巻（web）で確認できる。` | `出所・引用はセッションの要件一覧（web）で確認できる。` | 出力フォーマット既定テンプレ（開発者向け）。**実体は backend `packages/sanba_shared/.../output_formats.py`**。共有パッケージ側で修正・要調整 |
| `未確定の論点があれば次回セッションで深掘りする` | `未確定の論点があれば次回セッションでさらに確認する` | 同上（企画者向け既定テンプレ・backend 由来。P2） |

**変更なし（確認済み）**: 見出し `アプリ管理`/`アプリ詳細`/`基本情報`/`アプリを登録`/`登録済みのアプリ`/`見つかりません`/`アプリの削除`/`要件結果の出力フォーマット`／
ラベル・ボタン `アプリ名（必須）`/`説明（任意）`/`URL キーワード（必須）`/`保存する`/`追加`/`＋ 登録する`/`＋ 招待する`/`＋ リンクを発行する`/`削除する`/`削除する…`/`やめる`/`外す`/`取り消す`/`失効する`/`デフォルトに戻す`/`デフォルトのフォーマットを見る`／
`前提リポジトリ`（既に統一済み）/`リポジトリ`/`ブランチ`/`既定はデフォルトブランチ`/`選択してください`／索引ステータス `未紐づけ`/`索引待ち`/`索引中…`/`索引済み`/`一部索引済み`/`索引失敗`／
招待ステータス `応答待ち`/`承諾済み`/`辞退`/`取り消し済み`/`期限切れ`/`招待の状況`／`対象`/`期限`/`回数上限`/`期限なし`/`制限なし`/`失効済み`/`使用:`／`読み込み中…`/`登録済み`/`デフォルト使用中`/`保存しました`。

### ヘルプ追加ポイント（すべて 通常）

| 位置（ラベルの隣・画面） | 見出し | 本文 |
|---|---|---|
| /products・/products/[id]「URL キーワード（必須）」 | URL キーワードとは | 要件インタビューを始めるページの URL に使う短い合言葉です（例: `/expense-app/prepare`）。小文字の英数字とハイフンで付け、全体で重複しない名前にします。あとから変えると URL も変わります。 |
| /products/[id]「前提リポジトリ」状態チップ | コードの読み込み（索引）とは | リポジトリの中身をあらかじめ読み込み、検索できる状態にすることです。会話中の質問の前提として使います。「索引済み」で準備完了、「索引中…」は処理中、「索引失敗」はもう一度の紐づけが必要です。 |
| /products/[id]「前提リポジトリ」見出し | 前提リポジトリとは | 紐づけたコードを読み込み、会話での質問の前提に使います。確定した要件を GitHub の Issue として書き出す先にもなります。 |
| /products/[id]「要件インタビューのリンク」の「対象」 | 対象（開発者向け／利用者向け）とは | リンクを開いた相手にどんな聞き方をするかの設定です。「開発者向け」は技術面をくわしく確認し、「利用者向け」は専門用語を避けて困りごとを聞き取ります。登録した利用者向け語彙もこの聞き分けに使います。 |
| /products/[id]「要件インタビュー中の確認項目」見出し | 確認項目とは | 会話のなかで必ず確認したいことを、あらかじめ登録しておく項目です。会話の流れの中で一つずつ確認し、要件結果の文書にも一覧が載ります。「対象」を選ぶと、その相手との会話でだけ確認します（全員＝どの会話でも確認）。 |
| /products/[id]「要件結果の出力フォーマット」入力欄 | 差し込みタグ（`{{ }}`）とは | 文書を作るときに実際の内容へ自動で置き換わる印です。`{{session_title}}`＝会話のタイトル、`{{app_name}}`＝アプリ名、`{{goal}}`＝準備画面で入れたゴール、`{{date}}`＝作成日、`{{requirements}}`＝確定した要件（優先度つき）、`{{requirements_plain}}`＝確定した要件（優先度なしのやさしい箇条書き）、`{{check_items}}`＝確認項目の一覧。未入力の項目は「（未設定）」になります。 |
| /products/[id]「要件結果の出力フォーマット」見出し | 出力フォーマットとは | 会話の結果をまとめる文書のひな形です。読み手（利用者・企画者・開発者）ごとに用意でき、登録しなければ標準のひな形が使われます。 |
| /products/[id]「利用者向け語彙」見出し | 利用者向け語彙とは | 利用者が実際に目にしている言葉（画面名や機能の呼び名）です。利用者向けの会話ではこの言葉で質問し、技術用語を避けます。 |

---

## S7. 招待・参加・システム画面・設定・ナビ
対象: `app/join/[token]/page.tsx`/`app/member-invites/[token]/page.tsx`/`MemberInviteNotices`/`app/error.tsx`/`app/not-found.tsx`/`AccessErrorScreen`/`app/settings/page.tsx`/`GitHubLinkCard`/`AppShell`/`SideMenu`/`AccountMenu`/`sanba/AppHeader`

### コピー変更表

| 現状の文言 | 正語(通常) | 正語(end_user) | 位置・表示条件 |
|---|---|---|---|
| `深掘りに参加` | `会話に参加` | 同左 | join AppHeader title。開始前でモード未確定のため中立の「会話」 |
| `深掘りを開始する`（aria） | `会話を始める` | 同左 | join 開始ボタン aria（可視「開始する」は変更なし） |
| `「{productName}」の深掘り` | `「{productName}」の要件インタビュー` | `「{productName}」の聞き取り` | join conn 確定後 ConversationStart goal。interviewMode 既知のため出し分け |
| `企画(PdM)` | `企画者` | 同左 | join `MODE_ROLE_LABEL.developer` → roleLabel |
| `顧客` | `利用者` | 同左 | join `MODE_ROLE_LABEL.end_user` → roleLabel |
| `{email} さんから、このアプリで要件サンバ（音声での要件深掘り）をするメンバーに招待されています。` | `{email} さんから、このアプリで要件インタビュー（音声で要件を整理していく会話）をするメンバーに招待されています。` | 同左（メンバー招待は企画/開発者向けの通常スコープ） | member-invites 承諾カード本文 |
| `承諾すると、そのアプリで要件サンバ（深掘りセッション）ができるようになります。` | `承諾すると、そのアプリで要件インタビュー（音声で要件を整理する会話）ができるようになります。` | 同左 | MemberInviteNotices 説明文 |
| `✦ しくじり ✦` | `エラー`（装飾記号 ✦ 削除） | 同左 | error.tsx 上部の小ラベル |
| `新しい版が出ております` | `新しいバージョンがあります` | 同左 | error.tsx CardTitle（isChunkError） |
| `問答の間で支障が生じました` | `問題が発生しました` | 同左 | error.tsx CardTitle（通常） |
| `画面の版が更新されました。再読込でお直りいたします。` | `新しいバージョンに更新されました。再読み込みしてください。` | 同左 | error.tsx 本文（isChunkError） |
| `予期せぬ支障が生じました。お手数ですが再度お試しください。` | `予期しないエラーが発生しました。お手数ですが、もう一度お試しください。` | 同左 | error.tsx 本文（通常） |
| `再び試みる` | `もう一度試す` | 同左 | error.tsx reset ボタン |
| `再読込する` | `再読み込み` | 同左 | error.tsx リロードボタン（表記統一） |
| `頁が見つからず首をかしげる棒人間` | `ページが見つからないことを表すイラスト` | 同左 | not-found.tsx Figure label（aria）。イラスト自体は維持 |
| `この頁は見当たりませぬ` | `ページが見つかりません` | 同左 | not-found.tsx 見出し h1 |
| `お探しの頁は移されたか、はじめから無かったのかもしれません。` | `お探しのページは、移動または削除された可能性があります。` | 同左 | not-found.tsx 本文 |
| `…このアプリの壁打ちに参加するには、アプリのオーナーからメンバー招待を受ける必要があります。` | `…このアプリの要件インタビューに参加するには、…` | `…このアプリの聞き取りに参加するには、…` | AccessErrorScreen 補足文 |
| `録音と要件は最大 {RETENTION_DAYS} 日保持し、保存前に個人情報をマスクします。` | `…保存前に個人情報を伏せ字にします。` | 同左 | settings データの取り扱い（マスク→伏せ字。join 同意文・辞書と一致） |
| `セッション`（CardTitle） | `ログイン状態` | 同左 | settings ログアウトカード見出し（会話セッションと紛らわしい語） |
| `⎋ ログアウト` | `ログアウト` | 同左 | settings ログアウトボタン（不明瞭な ⎋ 記号削除） |
| `リポジトリを 1 つ紐づけると、要件サンバの深掘り時に前提情報として扱えます。` | `リポジトリを 1 つ紐づけると、要件インタビューのときに会話の前提として使えます。` | 同左 | GitHubLinkCard 説明文 |

**変更なし（確認済み）**: join `classifyJoinError` 全メッセージ（`参加にはログインが必要です`/`リンクの期限が切れています`/`リンクは無効化されています`/
`リンクの利用上限に達しています`/`リンクが正しくありません`/`リンクが見つかりません`/`混み合っています`/`同意が必要です`/`開始できませんでした` と各説明文）／
join 同意ゲート一式（`はじめる前にご確認ください`ほか箇条書き、`上記を確認し、録音と AI による整理に同意します。`）、`リンクから会話に参加します`、`開始する`/`準備しています…`/`ログインして参加する`/`ホームへ戻る`／
member-invites `STATUS_MESSAGE` 各文・`招待リンクが無効です`/`辞退しました`/`招待を承諾する`/`辞退する`/`処理しています…`・エラー各文・AppHeader `メンバー招待`／
MemberInviteNotices `招待が届いています`/`{name} のメンバーに招待されています`/`招待した人: …`/`承諾する`/`辞退する`／
AccessErrorScreen `アクセスできません`・存在秘匿本文／settings `プロフィール`/`データの取り扱い`/`保持日数`/`最大 N 日`／
AppShell/SideMenu ナビ（`ホーム`/`過去の要件一覧`/`アプリ管理`・開閉/閉じる aria・`折りたたむ`/`メニュー`）／
AccountMenu（`アカウントメニュー`/`ログイン中: …`/`アカウント設定`/`ログアウト`）／`sanba/AppHeader`（`戻る`）／
GitHubLinkCard（`GitHub 連携`/`連携中`/`連携を解除`/`GitHub と連携する`・エラー各文）。

### ヘルプ追加ポイント

| 位置（ラベルの隣・画面） | 見出し | 本文 | 対象 |
|---|---|---|---|
| member-invites 承諾カード見出し＋MemberInviteNotices「招待が届いています」＋AccessErrorScreen 本文の「メンバー招待」 | メンバー招待とは | アプリに継続して関わるメンバー（企画者・開発者）を追加する招待です。承諾するとログイン中のアカウントがメンバーになり、そのアプリで何度でも要件インタビューを開けます。1 回かぎりの参加リンクとは別のものです。 | 通常 |
| join 参加画面「リンクから会話に参加します」 | 参加リンクとは | 発行された URL から、その場で 1 回だけ会話に参加できるリンクです。事前の入力やアプリ登録は要りません。継続してアプリに関わるメンバー招待とは違い、この会話かぎりの参加です。 | 両モード（文面は平易側） |
| join 参加画面「はじめる前にご確認ください」 | 録音とデータの扱い | 会話は録音し、内容の整理に AI を使います。録音と要件は最大30日で自動的に削除し、保存前に個人情報を伏せ字にします。 | end_user |
| settings「保持日数」 | データの保持とは | 録音と要件は最大30日で自動的に削除します。保存する前に、お名前や連絡先などの個人情報を伏せ字にします。 | 通常 |
| settings GitHubLinkCard「GitHub 連携」見出し | 前提リポジトリとは | 紐づけたコードを読み込み、会話での質問の前提に使います。確定した要件を GitHub の Issue として書き出す先にもなります。 | 通常 |

---

## 総括メモ（横断調整・実装時の注意）

- **ヘルプ本文の集約**: 同一用語（前提リポジトリ・確信度/確からしさ・出所/だれの発言か・索引・出力フォーマット・メンバー招待/参加リンク）は
  複数画面で再掲される。`lib/help/` の辞書に一度だけ定義し、各画面はキー参照する（重複記述を作らない）。
- **共有コンポーネントの重複**: 「確信度」ヘルプは `RequirementsScrollList`（S4）を結果画面（S5）が再利用するため、実装は共通化する。
- **共有ラベル定数**: 検知/カテゴリ/MoSCoW/audience は `lib/realtime/mapping.ts`・`lib/audience.ts` に集約済み。S0 基盤でここを正語化し各画面へ波及させる。
- **backend 由来の露出文字列（S6）**: 出力フォーマット既定テンプレの `絵巻`/`深掘り` は `packages/sanba_shared/.../output_formats.py`。web と別 PR（共有パッケージ）で対応。
- **要最終確認（判断項目）**: 開始行為の呼称統一（壁打ち/要件サンバ/深掘りの排除）、`深掘りリンク→要件インタビューのリンク` の改名、
  `未確定→未解消` の統一、選択肢観点 `どう並ぶ？(how)` の名詞ラベル、`ChatBubble` グリフ `産`/`企` の中立化（P2）。
