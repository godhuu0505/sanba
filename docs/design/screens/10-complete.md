# 10 完了（オーレ！）

- **優先度**: P1
- **Figma 正本**: node `31:313`（10 完了・オーレ！）

## 目的
セッションの締め。成果（矛盾の裁定・抜けの発見・Issue 化）を祝祭的に提示し、次のアクション（再実行）へ送る。
「解像度を上げる所作が完了した」体験の幕引き（アートディレクション §0 の "黄金が爆ぜる" 緩急の急）。

## UI 要素（Figma 由来）
- 中央コピー: 「✦ 要件、産まれました ✦」/ 大見出し「オーレ！」。
- サマリ文: 「矛盾2を裁定し、抜け4を取り上げ、6件をIssue化しました。」
- 主 CTA: **「もう一度はじめから」** → 01 ホーム/02 準備へ。
- （09 から引き継いだ成果物リンクがあれば併記: 作成された Issue/ドキュメントへの導線。）

## 状態 / 遷移
- 入力: `session.completed`(summary{contradictions_resolved, gaps_found, issues_created}, artifacts[]).
- 数値は session.completed のサマリと一致（クライアントで再集計しない）。
- 遷移: 「もう一度はじめから」→ ルーム切断 → 01/02 へ。成果物リンクは新規タブで開く。

## 連携
- データチャネル: `session.completed`（agent が終了時に publish）。
- 成果物: 09 の `POST /export` が返した `issue_url` / `doc_url` を表示（あれば）。
- セッション終了処理（ルーム退出・録音停止）が確実に走る。

## 受け入れ基準（AC）
- [ ] サマリの件数が `session.completed` の値と一致する。
- [ ] 成果物（Issue/doc）リンクがあれば表示され、無くても完了画面は成立する。
- [ ] 「もう一度はじめから」でルームを正しく切断し、初期画面へ戻る。
- [ ] 祝祭演出（オーレ！）は完了時のみで、常用画面の認知負荷を上げない（§1 緩急）。
- [ ] コピーは現代語ベース（「オーレ！」はブランド表現として可、古語ボイスは不採用）。

## 現状差分
- **未実装**。セッション終了の専用画面・成果物リンク表示が無い。
- `session.completed` データチャネルの publish/購読を追加。

## 根拠
体験の完結に必要だが、核の検知・要件（05/08/09）の後に来る仕上げのため P1。
